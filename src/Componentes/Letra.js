import React from 'react';

    const Letra = ( props ) => {
        return (
            <div className="Letra">
          {/*       <p onClick={props.click}>Soy {this.props.Letra}</p>
                <p>{props.children}</p> */}
                <p>Soy una letra!</p>
            </div>
        )
    };


export default Letra;